import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'l02-components',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
